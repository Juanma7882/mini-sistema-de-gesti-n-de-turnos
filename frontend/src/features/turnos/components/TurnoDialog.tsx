// TODO(frontend.md §6.1 / §6.3): form crear/editar turno (solo Admin) con
// buscador de paciente, select de profesional, fecha + hora → POST/PUT /turnos.
// Maneja 409 (slot ocupado) inline con useProblemForm.
export function TurnoDialog() {
  return null
}
