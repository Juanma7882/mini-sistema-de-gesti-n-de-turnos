# URL base de la API (sin barra final). Ver README.md → Variables de entorno.
VITE_API_URL=http://localhost:5000/api

# Opcional, solo dev: prellena el login con credenciales de demo. Dejar vacío
# para que el form arranque vacío.
VITE_DEMO_EMAIL=
VITE_DEMO_PASSWORD=
